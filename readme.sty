A PDF file is initially used to store photos. This project extracts data from a PDF file.

This can be run on your machine by activating the virtual environment name.
 The required libraries are also installed.
Since this project uses Tesseract, you must add its path to your environment variables
 on your machine in order to run it .

The rest of how the project works is described in the project report.
